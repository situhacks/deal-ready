reportlab>=4.0
matplotlib>=3.8
pypdf>=4.0
pymupdf>=1.24
numpy>=1.26

# Local models are optional and feature-gated. Absent = that backend reports
# "not run" rather than scoring zero.
#   Ollama + minicpm-v4.6  parse backend C (vision). 1.6GB pull.
#   Ollama + qwen3.5:4b    escalation tier, for charts with no data labels.
#   Ollama + nomic-embed-text  page routing.
#   pytesseract + Tesseract binary  parse backend B (OCR baseline).
